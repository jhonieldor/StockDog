'use strict';

/**
 * @ngdoc service
 * @name stockDogApp.Vendas
 * @description
 * # Vendas
 * Service in the stockDogApp.
 */
angular.module('stockDogApp')
  .service('Vendas', function Vendas() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
